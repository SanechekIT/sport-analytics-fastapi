from pydantic import BaseModel

class MealItemCreate(BaseModel):
    name: str
    calories: int
    protein: float
   

class MealItem(MealItemCreate):  # Наследует все поля из Create
    id: int 

    class Config:
        orm_mode = True 
